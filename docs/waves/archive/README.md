# Waves Archive

This folder stores historical snapshots and superseded planning/audit docs.

Policy:

- Keep active execution and traceability docs in `docs/waves/` root.
- Move date-stamped snapshots, superseded drafts, and one-off prompt/context packs here.
- Preserve links from active docs when archived artifacts are still used as historical evidence.
